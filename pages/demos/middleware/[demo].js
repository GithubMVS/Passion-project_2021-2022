const Demo = () => {

  return (
    <div>
      <p>Demo Middleware</p>
      <h1> In middleware <b>you can run code before a request is completed.</b> Based on the user`s incoming request, <b>you can modify the response</b> by rewriting, redirecting, adding headers, or even streaming HTML. </h1>
    </div>
  )
}

export default Demo
